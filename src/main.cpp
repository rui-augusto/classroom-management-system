#include "main.hpp"

int main(){

    Menu menu;
    while (true){
        menu.showMenu();
    }

    // TODO:
    // ? show all students or show only one by index?
    // * ask what the manager wants to do in 'show' function

    return 0;
}
